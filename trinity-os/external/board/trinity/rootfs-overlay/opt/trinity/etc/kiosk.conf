# Конфигурация киоска Trinity OS L0.
# KIOSK_URL — что открывать на весь экран при загрузке.
# По умолчанию — Trinity Browser (busybox httpd раздаёт /opt/trinity/app).
# Чтобы сменить приложение ОС — просто замените URL.
KIOSK_URL="http://127.0.0.1:8080/"

# Рендерер cog: gles (аппаратный, авто-фолбэк на софт при падениях) | modeset
#KIOSK_RENDERER=gles
